# Validate catches using quality flags

Applies validation flags and filters out problematic catches based on
quality checks. Propagates flags to survey level to ensure consistency.

## Usage

``` r
validate_catches(catch_data)
```

## Arguments

- catch_data:

  Processed catch data

## Value

A dataframe of validated catches
