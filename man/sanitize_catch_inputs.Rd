% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey-reshaping.R
\name{sanitize_catch_inputs}
\alias{sanitize_catch_inputs}
\title{Sanitize physical catch inputs}
\usage{
sanitize_catch_inputs(df)
}
\arguments{
\item{df}{A data frame containing any subset of the columns above.}
}
\value{
The input with out-of-range values replaced by NA.
}
\description{
Drops (sets to NA) values for \code{length}, \code{weight_bucket}, \code{n_buckets}, and
\code{individuals} that fall outside physically plausible bounds. Defensive
against unit errors (length in metres instead of cm, weight_bucket in
grams instead of kg), decimal-separator confusion ("25.000" parsed as
25000 instead of 25.0), and obvious data-entry slips.
}
\details{
Bounds are conservative — they remove only the unambiguously impossible.
Downstream validation applies stricter, taxon-aware checks; sanitization
here protects the preprocessed parquet against extreme outliers
(e.g. \code{length = 195000} cm collapsing a trip total to billions of kg
via \verb{W proportional to L^b} with b approximately 3).

Bounds:
\itemize{
\item \code{length} in (0, 300] cm — no fish or octopus exceeds approximately 300 cm
\item \code{weight_bucket} in (0, 100] kg — typical bucket is 5–30 kg
\item \code{n_buckets} in (0, 200] — productive seine landings reach approximately 50
\item \code{individuals} in (0, 10000] — large pelagic schools can be thousands
}

Columns absent from the input are left untouched.
}
\keyword{internal}
