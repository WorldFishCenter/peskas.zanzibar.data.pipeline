% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-taxa.R
\name{assert_taxa_coverage}
\alias{assert_taxa_coverage}
\title{Fail the run when a taxon loses its length-weight coefficients}
\usage{
assert_taxa_coverage(
  taxa_list,
  lw,
  exempt = c("MZZ", "UNK", "UNKN", "MAE", "TAG", "GQT", "CRA", "KAK", "LHV", "RMB",
    "RTY", "SSP")
)
}
\arguments{
\item{taxa_list}{Character vector of FAO 3-alpha codes requested.}

\item{lw}{The \code{lw} table from \code{\link[=getLWCoeffs]{getLWCoeffs()}}, after any manually curated
coefficients have been bound on.}

\item{exempt}{Codes that carry no coefficients today. This is a \strong{baseline,
not a whitelist}: it records the taxa that were already uncovered when the
check was introduced (measured 2026-09-05 against FishBase 25.04 /
SeaLifeBase 24.07, 120 of 134 codes resolving), so that any \emph{new} loss fails
the run. \code{CJX} and \code{PWT} are deliberately absent — they resolve at 25.04
and are the two codes that break at 26.06, so a release move fails here.
Shrinking this list is follow-up work; each group below is a separate fix.

\describe{
\item{Not a taxon}{\code{MZZ} and \code{UNKN} are dropped by \code{\link[=get_fao_groups]{get_fao_groups()}}
before the search runs; \code{UNK} is absent from ASFIS entirely and is
rewritten to the fish group's most frequent taxon later in
\code{preprocess_wf_surveys()}.}
\item{Wrong reference name}{The ASFIS name does not describe the animal
landed in Zanzibar, so the area 51 filter correctly removes it.
\code{MAE} and \code{TAG} name species absent from FAO 51. \code{AHI}, \code{BFL} and
\code{MAC} were the same kind of error and are now remapped on read in
\code{reshape_catch_data()} -- to \code{BAF}, \code{TEI} and (for the sharks-and-rays
group) \code{AQX} -- so they no longer reach this check.}
\item{No published coefficients}{\code{GQT} (\emph{Plectorhinchus gaterinus})
resolves to a species that does occur in FAO 51, but FishBase carries
no length-weight pair for it in any length type. There is nothing to
convert and nothing to alias; the measurement does not exist.}
\item{Outdated synonym}{Names valid when ASFIS was written and since
synonymised are now corrected in \code{\link[=taxa_search_aliases]{taxa_search_aliases()}}, which
recovered \code{CLP}, \code{ESR}, \code{LZV}, \code{OQC}, \code{RPO} and \code{VMX}. Only \code{CRA}
(\emph{Brachyura}) is left: an infraorder, a rank the backbone omits.}
\item{No TL-type coefficients}{The species resolves and occurs in FAO 51
and has published (a, b) pairs, but every one is fork, standard or
another length type. \code{\link[=get_length_conversions]{get_length_conversions()}} now restates most of
these on a total-length basis, which recovered 15 codes including
swordfish, the tunas, the marlins and the trevallies. The five left
are \code{KAK}, \code{LHV}, \code{RMB}, \code{RTY} and \code{SSP}, which have no usable
length-length fit to convert through.}
}}
}
\value{
\code{lw}, invisibly.
}
\description{
The FishBase read is a live network read of a remote parquet dataset. A new
release therefore reaches the pipeline the moment a container is rebuilt,
with no code change. Release 26.06 dissolved \code{Caesionidae} into \code{Lutjanidae}
and \code{Scaridae} into \code{Labridae}; both family names survive with \strong{zero
species} in them, so any taxon whose reference name is one of those families
expands to nothing and gets no coefficients.

Nothing fails on its own when that happens: \code{\link[=calculate_catch]{calculate_catch()}} left-joins
the coefficients, so a taxon with no \verb{(lw_a, lw_b)} pair yields \code{NA} weight,
and \code{NA} sums to zero. The taxon disappears from the portal and the run stays
green. This turns that silence into a failed job.
}
\section{Porting}{
 The check transfers unchanged, but \code{exempt} is
country-specific. Run once against the country's own taxa list and record
whatever it reports as the starting baseline.
}

\keyword{mining}
\keyword{preprocessing}
