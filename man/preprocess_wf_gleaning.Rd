% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process-catch-gleaning.R
\name{preprocess_wf_gleaning}
\alias{preprocess_wf_gleaning}
\title{Pre-process WorldFish Zanzibar Gleaning Surveys}
\usage{
preprocess_wf_gleaning(log_threshold = logger::DEBUG)
}
\arguments{
\item{log_threshold}{Logging threshold level (default: \code{logger::DEBUG}).}
}
\value{
A data frame of preprocessed gleaning surveys: one row per
submission x shell group x size class, with general/demographic/activity/
supply-chain fields plus \code{total_individuals} and \code{total_catch_kg}.
}
\description{
Downloads raw structured gleaning survey data from cloud storage and
preprocesses it into a single analysis-ready data frame. The function
assembles three pieces and joins them on the submission:
\enumerate{
\item \strong{General info} -- strips the Kobo group prefixes
(\verb{group_general/}, \verb{group_trip/}, \verb{no_fishers/}, \verb{demographics/},
\verb{group_gleaning_activity/}, \verb{group_supply_chain/}), selects and
renames the trip, demographic, activity and supply-chain fields,
coalesces the conditional \code{landing_site} columns into one, and
coerces dates and numeric fields.
\item \strong{Catch info} -- reshapes the wide \code{group_catch} block into a
tidy long table (one row per submission x shell group x size class)
via \code{\link{reshape_gleaning_catch}}, unifying the parallel
bucket/plastic container fields and applying conservative
sanitisation.
\item \strong{Catch totals} -- per submission, sums individuals across
size classes (\code{total_individuals}) and reconstructs catch weight as
\code{unit_weight_kg * n_containers} (\code{total_catch_kg}); the container
fields are constant within a submission, hence \code{first()}.
}
}
\details{
Configurations are read from \code{config.yml} with the following necessary
parameters:

\if{html}{\out{<div class="sourceCode">}}\preformatted{surveys:
  wf_gleaning:
    raw:
      file_prefix:
      version:
storage:
  google:
    key:
    options:
      project:
      bucket:
      service_account_key:
}\if{html}{\out{</div>}}

The function uses logging to track progress.
}
\seealso{
\code{\link{reshape_gleaning_catch}}, \code{\link{sanitize_gleaning_inputs}}
}
\keyword{preprocessing}
\keyword{workflow}
