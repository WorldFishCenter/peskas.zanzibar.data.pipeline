% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process-catch-gleaning.R, R/survey-reshaping.R
\name{reshape_gleaning_catch}
\alias{reshape_gleaning_catch}
\title{Reshape Gleaning Catch Data from Wide to Long Format}
\usage{
reshape_gleaning_catch(data = NULL)

reshape_gleaning_catch(data = NULL)
}
\arguments{
\item{data}{A data frame of the gleaning survey export. Catch columns are
expected with the raw Kobo prefixes (\code{group_catch/...}, including the
nested \code{group_catch/group_length_gastropods/...} /
\code{group_catch/group_length_bivalves/...}).}
}
\value{
A long data frame with one row per submission x shell group x size
class (plus a single context row for submissions without shell detail).

A long data frame with one row per submission x shell group x size
class (plus a single context row for submissions without shell detail).
}
\description{
Reshapes the \code{group_catch} section of the Zanzibar/Pemba intertidal gleaning
KoboToolbox survey into a tidy long format. Unlike the boat-fishery surveys
(see \code{reshape_catch_data_v2()}), \code{group_catch} is a \emph{single} (non-repeated)
group per submission, so there is no \code{n_catch} index. The wide-to-long work
instead unpacks three layered structures:

Reshapes the \code{group_catch} section of the Zanzibar/Pemba intertidal gleaning
KoboToolbox survey into a tidy long format. Unlike the boat-fishery surveys
(see \code{reshape_catch_data_v2()}), \code{group_catch} is a \emph{single} (non-repeated)
group per submission, so there is no \code{n_catch} index. The wide-to-long work
instead unpacks three layered structures:
}
\details{
\enumerate{
\item \strong{Counting method.} \code{counting_method} (\code{backet} / \code{plastic_bag})
populates two mutually exclusive sets of columns (\verb{bucket_*} vs
\verb{plastic_*}). These are coalesced into one unified container block:
\code{container_type}, \code{container_size}, \code{container_size_kg},
\code{catch_fraction}, \code{unit_weight_kg}, \code{n_containers}.
\item \strong{Shell group + species.} \code{shell_group}
(\code{bivalves} / \code{gastropod} / \code{both} / \code{others}) drives which species
multi-selects (\code{Group_Bivalves}, \code{Group_Gastropod}) and which length
block apply. Species stay as a space-separated code list per group
(no per-species quantities exist in the instrument), with a derived
\code{n_species} token count.
\item \strong{Length frequency.} Individual counts by size class
(\verb{<5}, \code{5-15}, \verb{>15} cm) are recorded once per shell GROUP, not per
species. Each present group emits one row per size class.
}

Output grain: one row per \code{submission_id} x \code{group} x \code{size_class}. A
submission recorded under \code{bivalves} or \code{gastropod} yields 3 rows; \code{both}
yields 6; \code{others} or a missing \code{shell_group} (no shell detail) is preserved
as a single context row with \code{group}/\code{size_class}/\code{n_individuals} = NA so no
submission is silently dropped. All three size classes are retained per
present group (NA = not recorded, 0 = a recorded zero) so the size-frequency
distribution stays explicit and complete.

\enumerate{
\item \strong{Counting method.} \code{counting_method} (\code{backet} / \code{plastic_bag})
populates two mutually exclusive sets of columns (\verb{bucket_*} vs
\verb{plastic_*}). These are coalesced into one unified container block:
\code{container_type}, \code{container_size}, \code{container_size_kg},
\code{catch_fraction}, \code{unit_weight_kg}, \code{n_containers}.
\item \strong{Shell group + species.} \code{shell_group}
(\code{bivalves} / \code{gastropod} / \code{both} / \code{others}) drives which species
multi-selects (\code{Group_Bivalves}, \code{Group_Gastropod}) and which length
block apply. Species stay as a space-separated code list per group
(no per-species quantities exist in the instrument), with a derived
\code{n_species} token count.
\item \strong{Length frequency.} Individual counts by size class
(\verb{<5}, \code{5-15}, \verb{>15} cm) are recorded once per shell GROUP, not per
species. Each present group emits one row per size class.
}

Output grain: one row per \code{submission_id} x \code{group} x \code{size_class}. A
submission recorded under \code{bivalves} or \code{gastropod} yields 3 rows; \code{both}
yields 6; \code{others} or a missing \code{shell_group} (no shell detail) is preserved
as a single context row with \code{group}/\code{size_class}/\code{n_individuals} = NA so no
submission is silently dropped. All three size classes are retained per
present group (NA = not recorded, 0 = a recorded zero) so the size-frequency
distribution stays explicit and complete.
}
\examples{
\dontrun{
gleaning_long <- reshape_gleaning_catch(gleaning)

# Size-frequency by shell group across all submissions
gleaning_long |>
  dplyr::filter(!is.na(n_individuals)) |>
  dplyr::group_by(group, size_class) |>
  dplyr::summarise(total = sum(n_individuals), .groups = "drop")
}

\dontrun{
gleaning_long <- reshape_gleaning_catch(gleaning)

# Size-frequency by shell group across all submissions
gleaning_long |>
  dplyr::filter(!is.na(n_individuals)) |>
  dplyr::group_by(group, size_class) |>
  dplyr::summarise(total = sum(n_individuals), .groups = "drop")
}
}
\keyword{preprocessing}
\keyword{workflow}
