% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-taxa.R
\name{apply_taxa_aliases}
\alias{apply_taxa_aliases}
\title{Apply the search-name aliases to a processed species list}
\usage{
apply_taxa_aliases(species_list, aliases = taxa_search_aliases())
}
\arguments{
\item{species_list}{Output of \code{\link[=process_species_list]{process_species_list()}} before aliasing.}

\item{aliases}{Alias table, defaulting to \code{\link[=taxa_search_aliases]{taxa_search_aliases()}}.}
}
\value{
\code{species_list} with aliased rows substituted.
}
\description{
Replaces the ASFIS name and rank for any code in \code{\link[=taxa_search_aliases]{taxa_search_aliases()}},
leaving every other row untouched. A code with several aliases expands to
one row per alias, so all of them are searched and their coefficients
pooled.

\code{rank} is taken from the table rather than re-derived from the name: the
suffix rules in \code{\link[=process_species_list]{process_species_list()}} cannot recognise a bare genus like
\emph{Osteomugil}, which has no space and no \code{-idae} or \code{-formes} ending.
}
\keyword{mining}
\keyword{preprocessing}
