% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-taxa.R
\name{taxa_search_aliases}
\alias{taxa_search_aliases}
\title{Search names that override the ASFIS reference name}
\usage{
taxa_search_aliases()
}
\value{
A tibble of \code{a3_code}, \code{scientific_name} and \code{rank}. Several rows
may share an \code{a3_code}; all of them are searched.
}
\description{
A few ASFIS reference names match nothing in the taxonomic backbone, so the
taxon is dropped, gets no coefficients, and every catch row of it weighs
\code{NA} -- which sums to zero. This table substitutes a name that does match.
Each entry is a correction to the \emph{reference data}, not to FishBase.
}
\details{
\describe{
\item{\code{CLP}}{ASFIS calls it \code{Clupeidae}, but FishBase moved \emph{Sardinella},
\emph{Amblygaster} and \emph{Herklotsichthys} to \code{Dorosomatidae} in 2022, leaving
\code{Clupeidae} with 15 temperate species and \strong{none} in FAO area 51. The
dagaa landed in Zanzibar are Dorosomatidae. Searching the family rather
than the three genera makes no practical difference -- 31 g against
28 g for a 15 cm fish -- and matches the row already in Timor's
\code{taxa_search_aliases()}, so the pipelines agree.}
\item{Synonyms}{\code{ESR}, \code{RPO}, \code{LZV}, \code{OQC} carry names that were valid
when ASFIS was written and have since been synonymised. \code{VMX} is
\emph{Valamugil}, a genus the backbone no longer carries at all; its species
were split across \emph{Osteomugil} and \emph{Moolgarda} (6 species each at
release 25.04), so both are searched. \emph{Crenimugil} also absorbed some
but carries 0 species in the backbone, so listing it would only produce
a standing unmatched-name warning.}
}

\code{CRA} ("marine crabs nei", \emph{Brachyura}) is deliberately absent. Brachyura is
an infraorder and SeaLifeBase carries no rank between order \emph{Decapoda} and
family, so there is nothing to alias it to without deciding which crab
families Zanzibar actually lands. That is a local question, not a lookup.
}
\section{Porting}{
 Country-specific. The mechanism transfers unchanged; the
rows do not. Rebuild the table for each country's own taxa list.
}

\keyword{mining}
\keyword{preprocessing}
