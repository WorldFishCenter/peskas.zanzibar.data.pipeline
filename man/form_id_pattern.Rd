% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/airtable-helpers.R
\name{form_id_pattern}
\alias{form_id_pattern}
\title{Build a Form-ID Match Pattern for Airtable Asset Tables}
\usage{
form_id_pattern(form_ids)
}
\arguments{
\item{form_ids}{Character vector of Airtable form record IDs, e.g. the output
of one or more \code{\link[=get_airtable_form_id]{get_airtable_form_id()}} calls.}
}
\value{
A length-1 character string: a regex matching any of \code{form_ids} as a
whole element of a comma-separated list.
}
\description{
Builds the regular expression used to select the rows of an Airtable asset
table (\code{taxa}, \code{gear}, \code{vessels}, \code{sites}, \code{geo}) that belong to one or more
forms.
}
\details{
\code{form_id} in the assets snapshot is a \emph{linked-record} field. \code{\link[=airtable_to_df]{airtable_to_df()}}
collapses it with \code{paste(collapse = ", ")}, so a record shared by three forms
arrives as a single string \code{"recA, recB, recC"}. Selecting a form's records
therefore needs a whole-element match, not a substring test: a bare \code{recA}
would also match \code{recABC}.

Both degenerate inputs are rejected rather than tolerated, because both fail
silently downstream: a zero-length id makes \code{paste0()} recycle to a pattern
that matches only empty strings (every asset table comes back empty and every
join yields \code{NA}), and a multi-element pattern makes \code{\link[stringr:str_detect]{stringr::str_detect()}}
recycle element-wise against the data instead of testing alternatives.
}
\examples{
form_id_pattern("recAAAAAAAAAAAAAA")
form_id_pattern(c("recAAAAAAAAAAAAAA", "recBBBBBBBBBBBBBB"))

}
\keyword{helper}
\keyword{preprocessing}
