% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{create_geos}
\alias{create_geos}
\title{Generate Geographic Regional Summaries of Fishery Data}
\usage{
create_geos(monthly_summaries_dat = NULL)
}
\arguments{
\item{monthly_summaries_dat}{A data frame containing monthly fishery metrics by site}
}
\value{
This function does not return a value. It writes a GeoJSON file named
"zanzibar_monthly_summaries.geojson" to the inst/ directory of the package,
containing regional polygons with associated monthly fishery metrics.#'
}
\description{
This function creates geospatial representations of fishery metrics by aggregating
lnding sites data to regional levels along the Zanzibar coast. It assigns
each site to its nearest coastal region, calculates regional summaries of fishery
performance metrics, and exports the results as a GeoJSON file for spatial visualization.
}
\details{
The function performs the following operations:
\enumerate{
\item \strong{BMU Coordinate Extraction}: Retrieves geographic coordinates (latitude/longitude) for all landing sites
\item \strong{Spatial Conversion}: Converts sites coordinates to spatial point objects.
\item \strong{Regional Assignment}: Uses spatial analysis to assign each BMU to its nearest coastal region.
\item \strong{Regional Aggregation}: Calculates monthly summary statistics for each region by aggregating BMU data.
\item \strong{GeoJSON Creation}: Combines regional polygon geometries with summary statistics and exports as GeoJSON.
}

\strong{Calculated Regional Metrics} (using median values across BMUs in each region):
\itemize{
\item Mean CPUE (Catch Per Unit Effort, kg per fisher)
\item Mean RPUE (Revenue Per Unit Effort, currency per fisher)
\item Mean Price per kg of catch
}
}
\note{
\strong{Dependencies}:
\itemize{
\item Requires the \code{sf} package for spatial operations.
\item Requires a GeoJSON file named "ZAN_coast_regions.geojson" included in the
peskas.zanzibar.data.pipeline package.
\item Uses the \code{get_metadata()} function to retrieve sites location information.
}
}
\examples{
\dontrun{
# First generate monthly summaries
monthly_data <- get_fishery_metrics(validated_data, bmu_size)

# Then create regional geospatial summary
create_geos(monthly_summaries_dat = monthly_data)
}
}
\keyword{export}
