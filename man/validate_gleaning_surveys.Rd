% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-functions.R
\name{validate_gleaning_surveys}
\alias{validate_gleaning_surveys}
\title{Validate Preprocessed Zanzibar Gleaning Surveys and Build a Clean Dataset}
\usage{
validate_gleaning_surveys(log_threshold = logger::INFO)
}
\arguments{
\item{log_threshold}{Logging threshold (default \code{logger::INFO}).}
}
\value{
A list with \code{validated} (input + flag columns + alert fields),
\code{flagged_submissions} (one row per flagged submission with reasons),
\code{clean} (original columns, flagged submissions removed), and \code{summary}
(submissions tripping each check).
}
\description{
Flags unreasonable values in the preprocessed Zanzibar gleaning dataset (the
long skeleton from \code{preprocess_gleaning_surveys()}) and removes every
submission with at least one flag, since a bad value taints the whole record.
}
\details{
Tailored to the Zanzibar instrument. In addition to the demographic / effort /
economic / temporal range checks, it includes \strong{container-plausibility}
checks that target this pipeline's main weakness — catch weight is derived as
\code{unit_weight_kg * n_containers}, so single-container weights exceeding the
container's nominal capacity, or absurd container counts, are the root cause of
the heavy-catch tail:
\itemize{
\item \code{flag_unit_weight}  - \code{unit_weight_kg} > factor x \code{container_size_kg}
\item \code{flag_n_containers} - implausible number of full containers
}

Each check writes a \verb{flag_*} logical column (TRUE = problem; NA values pass).
Flags are consolidated per row into \code{alert_n}, \code{alert_flag}, \code{alert_reasons},
then rolled up to the submission for removal.
}
\examples{
\dontrun{
validate_gleaning_surveys()
v$summary
clean <- v$clean
}
}
\keyword{validation}
\keyword{workflow}
